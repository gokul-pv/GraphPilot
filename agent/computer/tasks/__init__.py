"""computer.tasks — standalone demos, one per cascade layer.

task_calculator.py  — Layer 2a (deterministic, zero vision calls)
task_notes.py       — Layer 2b (AX tree + LLM)
task_notion.py      — Layer 2c (Electron page)
task_vscode.py      — Layer 2c (Electron page) + Layer 3 (vision fallback)
"""
